---
import CategoryLayout from '@/layouts/CategoryLayout.astro'
import Card from '@/components/shared/Card.astro'

import categories from '@/data/categories'
---

<CategoryLayout>
	<div
		class='w-full grid grid-cols-1 gap-8 pb-12 md:grid-cols-2 lg:grid-cols-3'
	>
		{
			categories
				.filter((category) => !category.hide)
				.map((category) => (
					<>
						<Card
							image={{
								alt: `${category.title} thumbnail`,
								aspectRatio: 1.7777,
								src: category.image,
								width: 480,
							}}
							link={`/${category.type}`}
							title={category.title}
						/>
					</>
				))
		}
	</div>
</CategoryLayout>
